/* Responsive Navigation Bar */
.navbar {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  align-items: center;
  background-color: #0bd8c7;
  padding: 10px 20px;
}

@media (max-width: 768px) {
  .navbar {
    flex-direction: column;
    align-items: flex-start;
  }

  .dropbtn {
    width: 100%;
  }
}

/* Consolidated styles */
body, h1, h2, h3, p, .category-card {
  text-align: center;
}

/* Example of moving inline styles */
.image {
  margin: 0 auto;
  display: block;
  width: 90%;
}

.hero h1 {
  font-size: 48px;
  color: rgb(22, 88, 97);
}
